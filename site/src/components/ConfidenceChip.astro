---
interface Props {
  value: string | undefined;
}
const { value } = Astro.props;
const label = (value ?? '').replace(/_/g, ' ');
const cls = value ? `confidence confidence-${value.toLowerCase().replace(/_/g, '-')}` : 'confidence';
---
{value ? <span class={cls}>{label}</span> : null}
